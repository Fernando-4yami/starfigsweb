import CategoryPage from "@/components/category-page"
import { categoryConfigs } from "@/config/categories"

export default function FiguartsPage() {
  return <CategoryPage config={categoryConfigs.figuarts} />
}
